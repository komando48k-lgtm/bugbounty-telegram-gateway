[project]
name = "bugbounty-telegram-gateway"
version = "0.1.0"
description = "Telegram gateway for authorized bug-bounty workflow management"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
